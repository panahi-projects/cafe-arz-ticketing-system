import { Card } from "@mui/material";

const TicketReplyBox = () => {
  return (
    <Card
      sx={{
        height: "100%",
        overflowY: "auto",
        p: 2,
      }}
    >
      Reply box
    </Card>
  );
};

export default TicketReplyBox;
